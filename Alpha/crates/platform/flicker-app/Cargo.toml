[package]
name = "flicker-app"
version = "0.1.9"
edition.workspace = true
license.workspace = true

[dependencies]
flicker-core.workspace = true
flicker-input-core.workspace = true
flicker-input-router.workspace = true
# Platform input sources (KBM + gamepad) + the 120 Hz analog sampler. The winit
# event loop stays here; input translation moved to this crate (spec §6). The gilrs
# / GameController platform deps moved with the gamepad code and now live there.
flicker-input-device.workspace = true
flicker-render.workspace = true
flicker-2d.workspace = true
winit.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
pollster.workspace = true
anyhow.workspace = true
