[package]
name = "flicker-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# The toolkit now lives in the Alpha frontend cluster; this crate just re-exports it.
flicker-widgets.workspace = true
