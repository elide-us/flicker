[package]
name = "flicker-voxel"
version.workspace = true
edition.workspace = true
license.workspace = true

# Phase 1 is a pure-data crate. No third-party dependencies; standard library
# only. If a future phase needs one, add it to the workspace dependency table
# first and inherit from there.
[dependencies]
