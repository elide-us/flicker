[package]
name = "hex-map"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
flicker = { path = "../../crates/flicker" }
# The hex-world six-epoch world-gen stack, run over this map's ordering and
# drawn as each tile's terrain (terrain.rs).
flicker-materials.workspace = true
flicker-worldgen.workspace = true
anyhow.workspace = true
glam.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
