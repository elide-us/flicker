[package]
name = "voxel-cluster"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
flicker = { path = "../../crates/flicker" }
flicker-voxel.workspace = true
flicker-worker.workspace = true
anyhow.workspace = true
glam.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true

[dev-dependencies]
# Used only by the `ui` preview test to encode the procedural UI textures
# to PNG for visual inspection (run: `cargo test -p voxel-cluster ui_preview
# -- --ignored`).
image.workspace = true
