[package]
name = "voxel-cluster"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
flicker = { path = "../../crates/flicker" }
flicker-voxel.workspace = true
flicker-worker.workspace = true
anyhow.workspace = true
glam.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
serde.workspace = true
serde_json.workspace = true
# Decodes the intro-splash logo PNGs at runtime (assets/*.png); also used by the
# `ui_preview` test to encode the procedural UI textures.
image.workspace = true
