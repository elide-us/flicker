[package]
name = "hex-world"
version = "0.1.0"
edition.workspace = true
publish = false

# Stage 1 was the pure topology core (still here, still tested via `cargo test`).
# Stage 2 adds the render: each hex projected to a flat tangent facet on the
# virtual sphere, drawn as a whole faceted planet you orbit in god mode.
[dependencies]
flicker = { path = "../../crates/flicker" }
flicker-primitive.workspace = true
glam.workspace = true
anyhow.workspace = true
tracing-subscriber.workspace = true
